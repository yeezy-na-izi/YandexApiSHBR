version: '3.9'

services:
  api:
    build:
      context: .
      dockerfile: ./deploy/Dockerfile
    image: YandexApi:${YANDEXAPI_VERSION:-latest}
    restart: always
    env_file:
    - .env
    environment:
      YANDEXAPI_HOST: 0.0.0.0
      YANDEXAPI_DB_FILE: /db_data/db.sqlite3
    volumes:
    - YandexApi-db-data:/db_data/



volumes:
  YandexApi-db-data:
    name: YandexApi-db-data
